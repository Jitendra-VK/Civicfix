## Implemented Solution

CivicFix implements a centralized web-based solution where citizens can report civic issues and monitor their progress. Administrators can access submitted reports and update their status. The implemented system combines a React frontend, FastAPI backend, JWT authentication, SQLAlchemy ORM and PostgreSQL database to provide the required reporting and tracking workflow.